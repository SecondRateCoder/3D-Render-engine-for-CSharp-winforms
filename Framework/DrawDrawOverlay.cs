static class GizmoDraw{
    public static List<(Type t, object obj)> Content{get; private set;} = new List<(Type t, object obj)>();
    int ImgTranlate;
    ///<summary>Takes the input Bitmap, 
    /// scales down the existing Image and draw an translucent overlay atop it.</summary>
    public static Bitmap DrawOverlay(Bitmap bmp){
    }

}